package main

//"ejemplo1PostBasic/handlers"
//"net/http"

func main() {
	/*
		rt := chi.NewRouter()

		// Dependencies
		db := make(map[int]*handlers.Product)

		// Incializamos las dependencias del Controller con una base de datos vacia
		//pr :=
		//ct := handlers.ControllerProducts{
		//}


		rt.Post("/products", ct.Create())
	   	if err := http.ListenAndServe(":8080", rt); err != nil {
			panic(err)
	   	}
	*/
}
